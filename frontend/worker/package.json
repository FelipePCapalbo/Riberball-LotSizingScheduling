{
  "name": "riberball-lotsizingscheduling-worker",
  "private": true,
  "scripts": {
    "build": "npm install --prefix ../app && npm run build --prefix ../app",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^5.0.0"
  }
}
